# Work Computer Quick Start Guide (2026-05-15)

This guide outlines the steps to sync and resume work on this project when you arrive at your work computer.

## 1. Initial Sync (Pull Latest Changes)
Open PowerShell in your project directory (`D:\Projects\socialautopost`) and run:
```powershell
git fetch origin
git pull origin main
```
*Note: This ensures you have the latest Product Backlog, Worklogs, and code changes I just pushed.*

## 2. Environment Verification
Ensure your Python environment is ready:
```powershell
# Check Python version
python --version

# Verify required packages (if needed)
pip install -r PYTHON_ENV_REQUIREMENTS.md
```

## 3. GPU Runtime Check
Since this project uses GPU acceleration, verify the hardware is detected:
```powershell
# Run the GPU startup script
./start_with_gpu.ps1
```
Check the output files `gpu_app_8766.out` and `gpu_app_8766.err` for any initialization errors.

## 4. Staging Area Verification
If you are preparing for a "Shorts Native Staging" check:
1. Run the staging check script:
   ```powershell
   ./run_shorts_native_staging_check.ps1
   ```
2. Follow the checklist in `SHORTS_NATIVE_STAGING_CHECKLIST.md`.

## 5. Daily Objectives
Refer to `WORKLOG_2026-05-15.md` and `PRODUCT_BACKLOG.md` (which you just pulled) to see the pending tasks and the state where we left off.

---
*Generated by Gemini CLI Agent*
